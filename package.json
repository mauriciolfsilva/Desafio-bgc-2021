{
  "scripts": {
    "start": "clear; ./node_modules/.bin/serverless offline",
    "test": "clear; ./node_modules/mocha/bin/mocha $(find ./handler -name '*.js') --isMemoryDB --timeout 12000",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.39.0",
    "puppeteer": "^11.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "chrome-aws-lambda": "^10.1.0",
    "mocha": "^9.1.3",
    "serverless": "^2.65.0",
    "serverless-mocha-plugin": "^1.12.0",
    "serverless-offline": "^8.2.0",
    "sinon": "^12.0.1",
    "sinon-chai": "^3.7.0"
  }
}
